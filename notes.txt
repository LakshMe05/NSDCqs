devops mahamahams
Version control in git aan use cheyyane gooys
nthina? karanam storage purpose in aan
records changes to files so that you can recall specific versions later
track code changes, collaborate w/ teams, revert to prev if req
local version control thru revision
central version control thru cvs, SVN, Perforce
DISTRIBUTED thru git
CI: Push code frequently, automatically built and stored 
Version control or source code management is integration and management of your application's source code
version control 
kilukil pambaram ariya manasam ariyathambili urangu vavavo
jenkins->tools->nodejs->name vaka ppd, then v nodejs18->save and apply
jenkins->new item->name->pipeline->names ida->repo ida->branch ida->save and apply
scm->git->nnat aan repo
whenever error in build, look for version issues, directory and jenkins file location issues, package.json issues, 
HYPERVISOR, EC2, AMI,INSTANCE

HYPERVISOR : middle layer bw hardware and operating systems, creates, runs and manages vm

dev, pack, ship, run app : container: app code, dependencies, libraries, config files
easily shareable, load cheyth startup compared to VM is better, light weight
consistent env
docker follows client n server arch
rest api based calling for dcker daemons from client : build. run, pull
manages: container,img, network, volumes
compose tool : multi container handling tool for  docker

docker build .
docker build -t react-app .

docker registry : stores and distributes docker images : can store public or private images

docker image ls : list images
docker run image-id : run img 
docker run -p 3000:3000 image-id : run img w port
docker run -d -p 3000:3000 image-id : run img in background
docker ps : list running containers
docker stop container-name : stop container
docker  run -it image-id : run interactive container

kayin angane